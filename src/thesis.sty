\ProvidesPackage{thesis}

% Provides page numbers and headings on each page
%\pagestyle{plain}
%\pagestyle{headings}

% Packages
\usepackage{bold-extra}			% to allow boldface and small caps (see http://www.tex.ac.uk/cgi-bin/texfaq2html?label=bold-extras)
\usepackage[small,bf]{caption}	% for changing the appearance of captions
\usepackage{color}				% for highlighting
\usepackage{enumitem}  			% for nested enumerate
\usepackage{float}				% for preventing floating tables
\usepackage{ifpdf}				% to check if we are using latex/pdflatex
\usepackage{graphicx}			% for graphics
\usepackage{tabularx}			% for simple column stretching
\usepackage{titlepic}			% for an image on the title page
\usepackage{url}				% for web URLs
\usepackage{verbatim}			% for block commenting and code support
\usepackage{amsmath}            % for math support
\usepackage{amsfonts}           % for formatting mathematical symbols

\usepackage{verbatim, amssymb, amsthm} % For code environment and math support

% To allow single landscape pages
\ifpdf
\usepackage{pdflscape}			% for making single pages landscape
\else
\usepackage{lscape}				% for making single pages landscape
\fi

% Use an index
\usepackage{makeidx}
\makeindex

% Use a glossary
\usepackage[acronym,toc]{glossaries}
\makeglossaries

% To allow us to easily format a table row
% See http://www.latex-community.org/forum/viewtopic.php?f=5&t=3293
\usepackage{array}
\newcolumntype{+}{>{\global\let\currentrowstyle\relax}}
\newcolumntype{^}{>{\currentrowstyle}}
\newcommand{\rowstyle}[1]{\gdef\currentrowstyle{#1}%
#1\ignorespaces
}

% Variables (to save retyping and reformatting these throughout the document)
\newcommand{\thesisTitle}{Implementation of Random Projection Algorithms Using an FPGA}
\newcommand{\thesisDate}{2012}
\newcommand{\studentName}{Joshua Spence}
\newcommand{\studentID}{308216350}
\newcommand{\supervisorName}{Philip Leong}
\newcommand{\universityName}{University of Sydney}

% Custom commands
\newcommand{\highlight}[1]{\colorbox{yellow}{#1}}
\newcommand{\HRule}{\rule{\linewidth}{0.5mm}}

% Some acronyms
\newacronym{fpga}{FPGA}{Field-Programmable Gate Array}

% Some glossary entries
\newglossaryentry{Linux}
{
    description={is a generic term referring to the family of Unix-like
                 computer operating systems that use the Linux kernel},
   plural=Linuces
}
